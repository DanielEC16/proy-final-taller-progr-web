document.addEventListener("DOMContentLoaded", function () {
    // Inicializa Swiper
    var swiper = new Swiper(".swiper-container", {
        effect: "fade",
        loop: true,
        autoplay: {
            delay: 3000,
        },
    });
});